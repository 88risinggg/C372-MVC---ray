<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">

    <title>Product Details</title>
</head>

<body class="page">

    <!-- Navbar -->
    <%- include("partial/navbar", { 
      user, 
      brandHref: "/shopping", 
      navLinks: (user && user.role === "admin")
        ? [
            { href: "/inventory", label: "Inventory" },
            { href: "/products/admin/add", label: "Add Product" }
          ]
        : [
            { href: "/shopping", label: "Shop" },
            { href: "/cart", label: "Cart" }
          ] 
    }) %>

    <!-- Main Content -->
    <div class="container page-shell">
        <div class="page-header">
            <div>
                <p class="page-kicker">Product</p>
                <h1 class="page-title"><%= product ? product.productName : "Product Details" %></h1>
                <p class="page-subtitle">Details, pricing, and stock information.</p>
                <div class="page-meta mt-2">
                    <span class="meta-pill">Signed in as <span class="fw-semibold"><%= user.username %></span></span>
                </div>
            </div>
        </div>

        <% if (product) { %>

            <div class="card glass p-4 mx-auto content-narrow">

                <!-- Image -->
                <div class="text-center mb-3">
                    <img src="/images/<%= product.image %>"
                         class="rounded shadow-sm"
                         width="60%">
                    <% if (product.quantity <= 0) { %>
                        <div class="mt-2">
                            <span class="badge bg-danger">Out of Stock</span>
                        </div>
                    <% } %>
                </div>

                <!-- Details -->
                <p><b>Name:</b><br> <%= product.productName %></p>
                <p><b>Quantity:</b><br> <%= product.quantity %></p>
                <% const hasDiscount = product.discount && product.discount > 0; %>
                <% const basePrice = Number(product.price || 0); %>
                <% const finalPrice = hasDiscount ? basePrice * (1 - product.discount / 100) : basePrice; %>
                <p><b>Price:</b><br>
                  <span class="fw-semibold">$<%= finalPrice.toFixed(2) %></span>
                  <% if (hasDiscount) { %>
                    <span class="text-muted text-decoration-line-through ms-2">$<%= basePrice.toFixed(2) %></span>
                    <span class="badge bg-danger ms-1">-<%= product.discount %>%</span>
                  <% } %>
                </p>

            </div>

        <% } else { %>

            <p class="text-danger text-center">No product found.</p>

        <% } %>

        <!-- Back Button -->
        <div class="text-center mt-4">
            <a href="/inventory" class="btn btn-primary">Back to Inventory</a>
        </div>

    </div>

</body>
</html>
